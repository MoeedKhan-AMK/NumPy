# NUMPY CONCEPTS PROGRESS:
**1. NumPy Fundamentals**
- What NumPy is and why it is used
Difference between:
- Scalar, Vector, Matrix, 3D Matrix, and Tensor
-Why NumPy is faster than Python lists (vectorization + C backend)

**2. ARRAYS CREATION**
- Creating arrays from Python lists.
- np.array().
- Difference between Python list multiplication vs NumPy array multiplication.
- Understanding vectorization.

**3. Creating Arrays from Scratch**
- np.arange().
- np.linspace().
- np.zeros().
- np.ones().
- np.eye() (identity matrix).
- np.random.randn() .
- np.random.randint().

**4. Array Properties**
- Shape.
- ndim.
- dtype.
- size.
- itemsize.

**5. Array Reshaping**
- .reshape().
- .flatten().
- Reshape rules.

**6. Indexing & Slicing**
- Positive/negative indexing.
- Slicing arrays.
- Row & column slicing.
- Slicing matrices.
- Boolean indexing (conditions).

**7. Sorting in NumPy**
- np.sort().
- Array sorting without changing original.
- Sorting rows & columns.

**8. Fancy Indexing + np.where**
- Selecting elements using index lists.
- np.where(condition) → get indices.
- np.where(condition, x, y) → vectorized IF-ELSE.